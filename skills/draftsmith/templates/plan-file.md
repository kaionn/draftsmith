# plan ファイルテンプレート（plans/{task-slug}.md）

/draftsmith が設計確定後にリポジトリ内 `plans/` へ書き出す一時設計文書のテンプレート。
`{...}` を実際の内容で置き換える。冒頭の警告引用ブロックは**必ずそのまま残す**
（/plan-commit と人間が「一時文書である」ことを識別する目印になる）。

---

# Plan: {タスク名（20 字前後）}

> ⚠ 一時設計文書。/plan-commit でコミットメッセージに畳み込まれて削除される。
> repo ルートの Plans.md（タスク台帳）の代替ではない。

- Lane: {full | light}
- Status: {designed | implemented}
- Created: {YYYY-MM-DD}
- Source: {要件の出所。自然言語要件の一行要約、または Plans.md のタスク行の逐語引用}

## 目的

{なぜこの変更をするか。1〜3 文}

## 受け入れ基準

- AC-1: {検証可能な基準}
- AC-2: {...}

## 設計要旨

{変更対象ファイルの一覧と、それぞれをどう変えるかの要約。brief 全文の転記ではなく、
将来コミット履歴だけを読んだ人が変更の意図と構造を把握できる粒度で書く}

## mini-ADR

{full レーン: designer の mini-ADR（判断・文脈・却下した代替案）を転記。
light レーン: 「なし（方針一意のため light レーンで実施）」と書く}

## AI が下した判断

{タスク完了時（Step 7 / L5）に転記する。書き出し時点では
「（実装完了時に転記）」のプレースホルダを置く}
